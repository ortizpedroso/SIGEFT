# Cole em /etc/nginx/sites-available/sigep (Debian/Ubuntu) ou
# /etc/nginx/conf.d/sigep.conf e recarregue o Nginx.
#
# DNS: A  sigep.eventosbr.app.br  →  mesmo IP de www.eventosbr.app.br
# Certificado: certbot --nginx -d sigep.eventosbr.app.br
#
# NÃO substitua o vhost de www.eventosbr.app.br.

server {
    listen 80;
    listen [::]:80;
    server_name sigep.eventosbr.app.br;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name sigep.eventosbr.app.br;

    # Certbot preenche estes caminhos:
    ssl_certificate     /etc/letsencrypt/live/sigep.eventosbr.app.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sigep.eventosbr.app.br/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_read_timeout 120s;
    }
}
